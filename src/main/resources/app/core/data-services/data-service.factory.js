

export function DataServices($log, $q, $http, $rootScope, ApiConfig, BrowserConfig) {
    'ngInject';
    let DI = {
        $log,
        $http,
        $q,
        $rootScope,
        ApiConfig,
        BrowserConfig
    };
    return {
        
    };

}
